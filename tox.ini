[tox]
envlist = py310, py311, py312, py313

[testenv]
skip_install = true
deps =
    pytest
    pytest-cov
    pyfiglet
    click>=8.0
commands =
    pytest --cov=src --cov-report=term-missing --cov-report=xml
